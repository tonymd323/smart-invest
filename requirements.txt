# 智能投资系统依赖
# ── 核心依赖 ──────────────────────────────────────────────────────────────
akshare>=1.14.0
pandas>=2.0.0
numpy>=1.24.0
requests>=2.31.0

# ── 数据库（Python 内置 sqlite3，无需安装）────────────────────────────────
# sqlite3  # stdlib

# ── 测试 ─────────────────────────────────────────────────────────────────
pytest>=7.0.0

# ── 可选依赖 ─────────────────────────────────────────────────────────────
# tushare>=1.4.0   # 需要 token，按需启用
# schedule>=1.2.0  # Python 定时任务库（如不使用 crontab）

# ── Web 前端 ─────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn>=0.34.0
jinja2>=3.1.0
